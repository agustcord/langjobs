# ── Memoria local del proyecto (bóveda Obsidian) — NUNCA subir al repo ──
# El repo es PÚBLICO: la bóveda contiene log de sesiones, decisiones comerciales
# y estrategia de precios. Se listan todos los nombres que tuvo la carpeta
# (incluido el typo "obisidian", que es el nombre actual en disco).
.memory/
.extension_linkedin_obisidian/
.extension_linkedin_obsidian/

# ── Documentos de estrategia comercial e investigación interna — NO al repo ──
# El generador lleva el contenido de la estrategia embebido en el propio
# script, así que se excluyen tanto el PDF como su generador y notas internas.
LangJobs-Canales-Distribucion-Gratuita.pdf
LangJobs-Ban-Risk-Investigacion.md
tools/gen_distrib_pdf.py

# ── Datasets de Beta Testing y pruebas de campo locales ──
tests/beta_reports*.json
tests/field_fixtures.json
tests/baseline_snapshot.json

# ── Borradores de diseño y conceptos de íconos ──
tools/icon_concepts/
icono-langjobs-inskape.svg

# ── Sistema operativo ──
Thumbs.db
desktop.ini
.DS_Store

# ── Editores / IDEs ──
.vscode/
.idea/
*.swp

# ── Node (Fase 2 en adelante) ──
node_modules/
npm-debug.log*
package-lock.json.bak

# ── Builds / empaquetado de extensión ──
dist/
*.zip
*.crx
*.pem

# ── Secretos / entorno ──
.env
*.key

