<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <!-- TEMPLATE for LangJobs extension icon. Draw your concept inside the dashed SAFE AREA.
       Brand colors: blue #0a66c2 (ES), green #057642 (EN), white #ffffff.
       Keep shapes bold/central so they survive the 16px toolbar size. -->

  <!-- Solid rounded tile (blue). Edit fill if you prefer a white tile. -->
  <rect x="16" y="16" width="480" height="480" rx="104" ry="104" fill="#0a66c2"/>

  <!-- SAFE AREA guide: keep your main mark inside this box (~60% center) for 16px legibility.
       Delete this group before saving the final SVG. -->
  <g id="safe-area-guide" stroke="#ffffff" stroke-width="3" stroke-dasharray="14 10" fill="none" opacity="0.55">
    <rect x="120" y="120" width="272" height="272" rx="24" ry="24"/>
  </g>

  <!-- YOUR ART GOES HERE (between the tile and the guide, or replace the guide) -->
</svg>
